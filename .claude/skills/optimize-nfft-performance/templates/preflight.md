# Preflight — measurement track

- **Track:** <walltime | simulation>
- **Evidence** (the three CodSpeed checks, in order):
  - MCP tools present this session: <yes | no>
  - `codspeed status` logged in: <yes | no>
  - repo onboarded with a base-branch run: <yes | no>
- **Decision:** <three yes ⇒ simulation; any no ⇒ walltime — name the failing check>

<!-- Keep this short (≈3 lines of substance). It only records which track and why.
     Then set the tracker's Track field to the chosen track and flip the Preflight row to ✅. -->
